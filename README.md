# EventApp

EventApp е web приложение за управление на събития, разработено с Spring Boot. Приложението позволява на потребителите да създават, управляват и се записват за събития, като администраторите имат допълнителни права за управление на системата.

## 📋 Технологичен стек

- **Java:** 17
- **Spring Boot:** 3.4.0
- **Build Tool:** Maven
- **Database:** MySQL
- **Backend Framework:** Spring MVC
- **Frontend:** Thymeleaf templates
- **ORM:** Spring Data JPA
- **Security:** Spring Security
- **Logging:** SLF4J / Logback
- **Microservices:** Spring Cloud OpenFeign (за комуникация с rating service)
- **Maps:** Leaflet + OpenStreetMap (за избор на локация)

## 🎯 Основни функционалности

### 1. Управление на събития
- **Създаване на събитие** - Потребителите могат да създават нови събития с детайлна информация:
  - Име, описание, дати (начало и край), капацитет
  - Категория (само активни категории)
  - Локация с интерактивна карта (Leaflet + OpenStreetMap)
  - Изображение (избор от предварително заредени изображения)
- **Редактиране на събитие** - Създателите на събитията могат да редактират своите събития
- **Изтриване на събитие** - Създателите и администраторите могат да изтриват събития
- **Преглед на събития** - Потребителите могат да разглеждат всички налични събития:
  - Разделение на "Предстоящи" и "Изтекли" събития
  - Филтриране по категория
  - Сортиране по различни критерии
- **Записване за събитие** - Потребителите могат да се записват за събития
- **Отписване от събитие** - Потребителите могат да се отписват от събития
- **Преглед на участници** - Организаторите могат да виждат списък с всички записани участници

### 2. Рейтинги на събития
- **Оценяване на събития** - Потребителите могат да оценяват минали събития, за които са били записани
- **Преглед на рейтинги** - Показване на средна оценка и брой гласували
- **Защита срещу дублирани рейтинги** - Всеки потребител може да оцени всяко събитие само веднъж
- **Интеграция с микросървис** - Комуникация с `event-rating-svc` чрез OpenFeign

### 3. Управление на категории (Администратор)
- **Създаване на категория** - Администраторите могат да създават нови категории за събития
- **Деактивиране на категория** - Вместо изтриване, категориите се деактивират (soft delete)
- **Активиране на категория** - Администраторите могат да активират деактивирани категории
- **Защита срещу използване на неактивни категории** - Събития могат да се създават само с активни категории

### 4. Управление на потребители (Администратор)
- **Преглед на потребители** - Администраторите могат да виждат списък с всички потребители
- **Промяна на роля** - Администраторите могат да променят ролите на потребителите
- **Автоматично назначаване на ADMIN** - Първият регистриран потребител автоматично получава роля ADMIN

### 5. Профил на потребителя
- **Преглед на профил** - Потребителите могат да виждат собствения си профил
- **Редактиране на профил** - Потребителите могат да редактират собствената си информация
- **Визуализация на роля** - ADMIN потребителите виждат лилав badge "ADMIN" в профила си и на началната страница

## 🔐 Сигурност и роли

Приложението използва Spring Security за управление на достъпа:

### Роли
- **USER** - Обикновен потребител с достъп до основните функции
- **ADMIN** - Администратор с допълнителни права за управление
  - Първият регистриран потребител автоматично получава роля ADMIN
  - ADMIN ролята се визуализира с лилав badge в UI (home.html и profile.html)

### Типове endpoints
- **Open endpoints** - Доступни без аутентификация (`/`, `/register`)
- **Authenticated endpoints** - Достъпни за всички логнати потребители
- **Authorized endpoints** - Доступни само за администратори (`/admin/**`)

### Защита
- CSRF защита е включена
- Паролите се хешират с BCrypt
- Потребителите могат да редактират само собствения си профил
- Администраторите могат да управляват роли на други потребители

## 🗄️ База данни

- **Database:** MySQL
- **ORM:** Spring Data JPA
- **Primary Keys:** UUID за всички entities
- **Entity Relationships:**
  - Event ↔ Category (Many-to-One)
  - Event ↔ User/Creator (Many-to-One)
  - User ↔ Subscription (One-to-Many)
  - Event ↔ Subscription (One-to-Many)

### Entities
1. **User** - Потребители на системата
2. **Event** - Събития (с локация, координати, изображение)
3. **Category** - Категории на събития (с `isActive` флаг за soft delete)
4. **Subscription** - Записи на потребители към събития
5. **Ticket** - Билети за събития с QR код

## 📊 Scheduling & Caching

### Scheduled Jobs
- **cleanupPastEvents()** - Изпълнява се всеки ден в 02:00 с cron expression, изтрива събития по-стари от 2 дни след приключване

### Caching
Приложението използва Spring Cache за подобряване на производителността:
- **Cacheable операции:** Статистики (event count), Списък с категории
- **CacheEvict операции:** Автоматично изчистване на кеша при създаване/обновяване/изтриване на събития и категории

## 🔗 Микросървисна архитектура

Приложението комуникира с `event-rating-svc` микросървис за управление на рейтинги:
- **OpenFeign Client** - За REST комуникация с микросървиса
- **URL на микросървиса:** Конфигурира се в `application.properties` (`rating.service.url`)
- **DTOs** - Специални DTO класове за комуникация с микросървиса

## 📝 Logging

Всички валидни functionalities включват log statements:
- Успешни операции се логват на INFO ниво
- Грешки се логват на ERROR/WARN ниво
- Scheduled tasks се логват за проследяване на изпълнението

## 🧪 Testing

Приложението включва изчерпателни тестове, които покриват всички изисквания:

### Test Coverage
- **Line Coverage: 80.9%** (785 покрити, 185 пропуснати реда)
- **Instructions Coverage: 73%**
- **Branches Coverage: 50%**
- **Methods Coverage: 77.9%** (172 покрити, 38 пропуснати)
- **Classes Coverage: 96.2%** (26 покрити, 1 пропусната)

### Unit Tests
- **EventServiceTest** (21 теста) - Тества бизнес логиката на EventService
- **TicketServiceTest** (7 теста) - Тества бизнес логиката на TicketService
- **UserServiceTest** (15 теста) - Тества бизнес логиката на UserService

### Integration/API Tests
- **AdminCategoryControllerIntegrationTest** (5 теста) - Тества CategoryController с реална база
- **AdminUserControllerIntegrationTest** (5 теста) - Тества AdminUserController с реална база
- **EventControllerIntegrationTest** (7 теста) - Тества EventController с реална база
- **HomeControllerIntegrationTest** (7 теста) - Тества HomeController с реална база
- **IndexControllerIntegrationTest** (7 теста) - Тества IndexController с реална база

**Общо тестове:** 74 (всички минават успешно)

### Технологии за тестване
- **JUnit 5** - За писане на тестове
- **Mockito** - За мокиране на dependencies в unit тестове
- **Spring Boot Test** - За integration тестове с реална база
- **MockMvc** - За тестване на HTTP заявки в API тестове
- **JaCoCo** - За измерване на code coverage

### Стартиране на тестовете
```bash
# Стартиране на всички тестове
mvn clean test

# Генериране на coverage репорт
mvn clean test jacoco:report

# Coverage репортът се намира в: target/site/jacoco/index.html
```

## 🎨 Frontend

Приложението използва Thymeleaf за server-side rendering:
- **11+ динамични страници** - Всички страници са интерактивни
- **Responsive design** - Адаптивен дизайн за различни устройства
- **Тема (Light/Dark)** - Потребителите могат да превключват между светла и тъмна тема
- **Интерактивни карти** - Leaflet + OpenStreetMap за избор на локация
- **QR кодове** - Генериране на QR кодове за билети
- **ADMIN badge** - Визуализация на администраторската роля с лилав градиентен badge

### Web Pages
1. index.html - Начална страница със статистики
2. login.html - Страница за вход
3. register.html - Страница за регистрация
4. home.html - Табло на потребителя (с разделение на предстоящи/минали събития и рейтинги)
5. profile.html - Профил на потребителя
6. profile-edit.html - Редактиране на профил
7. events.html - Списък с всички събития (предстоящи и изтекли)
8. event-create.html - Създаване на ново събитие (с карта и избор на изображение)
9. event-edit.html - Редактиране на събитие
10. event-participants.html - Преглед на участници в събитие
11. admin-categories.html - Управление на категории (Админ)
12. admin-users.html - Управление на потребители (Админ)

## 🔧 Error Handling

Приложението включва глобална обработка на грешки:
- **GlobalExceptionHandler** - Централизирана обработка на изключения
- **Custom exceptions** - UserAlreadyExistsException, CategoryAlreadyExistsException
- **Built-in exception handlers** - IllegalArgumentException, IllegalStateException, MethodArgumentNotValidException
- **Validation messages** - Валидационни съобщения на български език
- **Специфична обработка за рейтинги** - Обработка на грешки от микросървиса с graceful degradation (връщане на празни резултати при недостъпен микросървис)

## 📦 Структура на проекта

```
EventApp/
├── src/
│   ├── main/
│   │   ├── java/main/
│   │   │   ├── controller/     # Controllers за обработка на заявки
│   │   │   ├── service/        # Business logic
│   │   │   ├── repository/     # JPA Repositories
│   │   │   ├── model/          # Entity класове
│   │   │   ├── config/         # Конфигурационни класове
│   │   │   ├── exception/      # Exception handling
│   │   │   ├── scheduled/      # Scheduled tasks
│   │   │   └── web/
│   │   │       ├── client/     # Feign клиенти за микросървиси
│   │   │       ├── dto/        # Data Transfer Objects
│   │   │       └── view/       # View модели
│   │   └── resources/
│   │       ├── templates/      # Thymeleaf templates
│   │       └── static/
│   │           ├── css/        # CSS стилове
│   │           └── images/     # Статични изображения
│   └── test/
│       └── java/main/
│           ├── controller/     # Integration/API тестове
│           └── service/         # Unit тестове
└── pom.xml
```

## 🚀 Стартиране на приложението

### Предварителни изисквания
- Java 17 или по-висока версия
- Maven 3.6+
- MySQL 8.0+
- `event-rating-svc` микросървис трябва да работи на порт 8081

### Инсталация

1. Клонирай репозитория:
```bash
git clone <repository-url>
cd EventApp
```

2. Конфигурирай базата данни в `application.properties`:
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/event_app_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=your_password
```

3. Конфигурирай URL на микросървиса (ако е различен):
```properties
rating.service.url=http://localhost:8081
```

4. Стартирай микросървиса `event-rating-svc` (виж README на микросървиса)

5. Стартирай приложението:
```bash
mvn spring-boot:run
```

6. Отвори браузър и отиди на:
```
http://localhost:8080
```

## 📋 API Endpoints

### Публични endpoints
- `GET /` - Начална страница
- `GET /register` - Страница за регистрация
- `POST /register` - Регистрация на нов потребител
- `GET /login` - Страница за вход

### Authenticated endpoints
- `GET /home` - Табло на потребителя
- `GET /profile` - Профил на потребителя
- `GET /profile/{id}` - Страница за редактиране на профил
- `POST /profile/{id}` - Обновяване на профил
- `GET /events` - Списък с всички събития
- `GET /events/create` - Страница за създаване на събитие
- `POST /events/create` - Създаване на ново събитие
- `GET /events/{eventId}/edit` - Страница за редактиране на събитие
- `POST /events/{eventId}/edit` - Обновяване на събитие
- `POST /events/{eventId}/subscribe` - Записване за събитие
- `POST /events/{eventId}/unsubscribe` - Отписване от събитие
- `POST /events/{eventId}/delete` - Изтриване на събитие
- `GET /events/{eventId}/participants` - Преглед на участници (само за организатор)
- `POST /ratings` - Създаване на рейтинг за събитие
- `GET /ratings/event/{eventId}` - Получаване на рейтинги за събитие (JSON)

### Admin endpoints
- `GET /admin/categories` - Управление на категории
- `POST /admin/categories` - Създаване на категория
- `POST /admin/categories/{id}/deactivate` - Деактивиране на категория
- `POST /admin/categories/{id}/activate` - Активиране на категория
- `GET /admin/users` - Управление на потребители
- `POST /admin/users/{id}/role` - Промяна на роля на потребител

## 🔗 Интеграции

- **Spring Security** - Аутентификация и авторизация
- **Spring Data JPA** - Доступ до базата данни
- **Spring Cache** - Кеширане на данни за подобрена производителност
- **Spring Scheduling** - Автоматично изпълнение на задачи по график
- **Spring Cloud OpenFeign** - REST клиент за комуникация с микросървиси
- **Thymeleaf** - Server-side rendering на templates
- **MySQL** - Релационна база данни
- **Leaflet + OpenStreetMap** - Интерактивни карти за избор на локация
- **QR Code Generation** - Генериране на QR кодове за билети

## 👥 Роли и права

### USER
- Създаване, редактиране и изтриване на собствени събития
- Записване и отписване от събития
- Преглед на всички събития (предстоящи и изтекли)
- Оценяване на минали събития, за които са били записани
- Преглед на участници в собствените си събития
- Управление на собствения профил

### ADMIN
- Всички права на USER
- Управление на категории (създаване, активиране, деактивиране)
- Управление на потребители и техните роли
- Изтриване на всяко събитие

## 📈 Статистики

Приложението предоставя статистики на началната страница:
- Общ брой събития
- Общ брой потребители
- Брой различни категории

## 🔄 Automated Tasks

- **Дневно изчистване** - Всеки ден в 02:00 се изтриват събития, които са завършили преди повече от 2 дни

## 📝 License

Този проект е част от учебна програма на SoftUni.

## 👨‍💻 Автор

Разработен като част от Spring Advanced курс в SoftUni.
